# OpenCode Config - Ignore patterns

# Secrets and credentials (never commit these)
.env
.env.*
*.key
*.pem
secrets/
credentials.json

# Node modules (plugin dependencies installed at runtime)
node_modules/

# OS files
.DS_Store
Thumbs.db

# Lock files (regenerated per-machine)
bun.lock
package-lock.json
yarn.lock
pnpm-lock.yaml

# Temporary files
*.tmp
*.temp
*.log

# Generated audit reports (point-in-time, not source-controlled)
.opencode/docs/SECURITY-AUDIT.md

# Eval run artifacts
opencode-eval-results/
opencode-eval-results-*/
